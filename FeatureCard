
import React from 'react';
import { View } from '../types';

interface FeatureCardProps {
  title: string;
  description: string;
  icon: string;
  view: View;
  onClick: (view: View) => void;
  color: string;
}

const FeatureCard: React.FC<FeatureCardProps> = ({ title, description, icon, view, onClick, color }) => {
  return (
    <div 
      onClick={() => onClick(view)}
      className="bg-white p-8 rounded-xl shadow-sm border border-slate-100 hover:shadow-2xl hover:border-amber-200 transition-all duration-500 cursor-pointer group relative overflow-hidden"
    >
      <div className="absolute top-0 right-0 w-24 h-24 bg-amber-50 opacity-0 group-hover:opacity-100 transition-opacity -mr-12 -mt-12 rounded-full"></div>
      
      <div className={`w-14 h-14 bg-slate-900 rounded-lg flex items-center justify-center mb-6 shadow-md group-hover:bg-amber-600 transition-colors text-2xl`}>
        {icon}
      </div>
      <h3 className="text-xl font-bold mb-3 text-slate-900 group-hover:text-amber-700 transition-colors">{title}</h3>
      <p className="text-slate-500 leading-relaxed mb-6 text-sm">{description}</p>
      
      <div className="flex items-center text-amber-600 text-xs font-bold tracking-widest uppercase group-hover:translate-x-[-4px] transition-transform">
        <span>استكشف الخدمة</span>
        <span className="mr-2">←</span>
      </div>
    </div>
  );
};

export default FeatureCard;
